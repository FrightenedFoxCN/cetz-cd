#import "../src/cetz-cd.typ": *

// first, one simple arrow

#cetz-cd(
    (($A$, $B$), ), (((arr("r", text: $f$), ), ),))

#cetz-cd(
    (($A$, ), ($B$, )), (((arr("d", text: $f$), ), ),))

#cetz-cd(
    (($A$, $$), ($$, $C$)), (((arr("dr", text: $f$), ), ),))